services:
  postgresdb:
    image: postgres
    container_name: postgres_db
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=Pass2020!
    volumes:
      - /volume/:/var/lib/postgresql/data
